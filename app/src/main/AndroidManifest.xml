<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_fc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fc_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".Getting_StartedActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserRegister"
            android:exported="false" />
        <activity
            android:name=".WebAssistantAuthActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".HomeActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".dectection.CameraActivity"
            android:exported="false" />
        <activity
            android:name=".SignScreen"
            android:exported="false" />
        <activity
            android:name=".OnboardingFinish"
            android:exported="false" />
        <activity android:name=".OnboardingActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>